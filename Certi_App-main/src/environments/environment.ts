// Este archivo es generado automáticamente por Angular CLI.
// Para producción, Angular usará environment.prod.ts si está configurado.

export const environment = {
  production: false,
  /** URL base de la API NestJS */
  apiUrl: 'http://localhost:3000/api',
};
